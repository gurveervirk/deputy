# deputy

`deputy` is a code intelligence CLI for querying and navigating code entities. It stores parsed symbols, types, imports, and module hierarchies in a local SQLite database and provides commands to search, inspect, and resolve entities across your project.